---
paths:
  - "src-tauri/src/commands/*.rs"
---

# Rust Command Modules

<!-- Generated by proofd. Rule ID: rust-command-modules. Do not edit manually. -->
<!-- Default Prefix: RU | Prefixes: RC, RU -->

- [RC-01] create_session / close_session -- Session CRUD. close_session does not persist; frontend owns persistence via persist_sessions_json.
- [RC-02] build_claude_args -- SessionConfig -> CLI args (--resume, --session-id, --project-dir, etc.)
- [RC-05] detect_claude_cli / check_cli_version / get_cli_help -- CLI discovery. spawn_blocking + CREATE_NO_WINDOW compliant.
- [RC-06] list_past_sessions -- Scan ~/.claude/projects/ for resumable sessions. Async with spawn_blocking. Head pass reads first 30 lines for firstMessage + sourceToolAssistantUUID; tail pass seeks last 256KB for lastMessage + model. Chain detection resolves sourceToolAssistantUUID to parent session via UUID map.
- [RC-08] persist_sessions_json / load_persisted_sessions -- Save/restore sessions. persist_sessions_json accepts frontend JSON directly (Rust-side metadata is stale).
- [RC-09] discover_builtin_commands / discover_plugin_commands -- Slash command discovery Tauri wrappers in cli.rs; sync implementations (discover_builtin_commands_sync, discover_plugin_commands_sync) live in crate::discovery (src-tauri/src/discovery/mod.rs). Two-step binary scan (name:"..." positions + forward/reverse brace-bounded window for descriptions). Filters: skips /-- prefixed names, skips descriptions containing 'tab ID' or 'DOM', requires cmd length 4-30 chars. Deduplicates by command name. Plugin discovery also returns rejection reasons logged as WARN events.
- [RC-16] read_claude_binary(cli_path) resolves Claude Code binary through 5-step chain: direct CLI path -> .cmd shim parse -> sibling node_modules -> legacy versions dir -> npm root -g. Implementation lives in crate::discovery (src-tauri/src/discovery/mod.rs); cli.rs re-exports and delegates to it. Enables slash command/settings discovery on standalone installs.
