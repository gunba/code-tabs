---
paths:
  - "src/main.tsx"
---

# src/main.tsx

Tag line: `L<n>`; code usually starts at `L<n+1>`.

## Project Structure

- [FS-01 L1] Frontend source tree: src/ contains main.tsx (entry), App.tsx (root layout), store/ (Zustand), hooks/ (React hooks), components/ (UI, co-located CSS), lib/ (pure logic, utilities, tap pipeline), types/ (TypeScript mirrors of Rust types)
