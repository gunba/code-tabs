---
paths:
  - "src-tauri/src/observability/codex_rollout.rs"
---

# src-tauri/src/observability/codex_rollout.rs

Tag line: `L<n>`; code usually starts at `L<n+1>`.

## Codex Rollout Observability

- [CR-04 L65] build_watcher() helper in codex_rollout.rs:L58 dispatches the notify backend by host OS to work around ReadDirectoryChangesW silently dropping events on OneDrive sync roots and redirected/roaming profiles where ~/.codex commonly lives on corporate Windows machines.
  - build_watcher<F: notify::EventHandler> returns Box<dyn notify::Watcher + Send>. On Windows: notify::PollWatcher::new(handler, Config::default().with_poll_interval(WINDOWS_POLL_INTERVAL=750ms)). On non-Windows: notify::RecommendedWatcher::new(handler, Config::default()) with 'use notify::Watcher as _' gated on cfg(not(target_os=windows)) so the trait is in scope for the trait-method call (PollWatcher::new is inherent). Box<dyn Watcher + Send> works because Watcher::new carries 'where Self: Sized' and is excluded from the vtable; .watch() dispatches via vtable without trait-in-scope. Both watcher sites (RolloutDirWatcher::start at L101 dir watcher + tail_rollout at L617 file watcher) route through build_watcher; run_rollout_dir_watcher's _watcher param widened to Box<dyn Watcher + Send>. Dir-watcher notify Err(e) emits eprintln (no AppHandle in scope, watcher shared across sessions). File-watcher notify Err(e) emits codex.rollout.notify_error WARN once per tail via record_backend_event, gated by a stack-local AtomicBool (notify_err_seen) moved into the FnMut closure where .swap(true, Relaxed) returns the prior value. run_watcher entry emits codex.rollout.watcher_armed DEBUG event recording resolved dir, cfg!(target_os=windows), and dirExistedBefore (captured before fs::create_dir_all) so observability.jsonl confirms the watcher armed against the expected path. EventKind::Modify(_)|Create(_) filter matches PollWatcher's *Kind::Any same as platform-native variants.
- [CR-01 L262] Codex rollout JSONL location: $CODEX_HOME/sessions/YYYY/MM/DD/rollout-<ts>-<uuid>.jsonl (CODEX_HOME defaults to ~/.codex; respects CODEX_HOME env override). File appended per-turn with lines: {timestamp, type: session_meta|response_item|event_msg|compacted|turn_context, payload}. This is the structured event source for Codex TUI session observability — no JSON-RPC or app-server attach.
- [CR-02 L385] notify-based watcher pattern in codex_rollout.rs: start_codex_rollout Tauri command creates a CodexRolloutHandle (oneshot stop channel), inserts it into CodexRolloutState registry before spawn (prevents start/stop race), then spawns run_watcher. run_watcher calls claim_unclaimed_rollout first (atomic: lock shared claimed_rollouts Arc<Mutex<HashSet>>, find fresh file, insert to claimed set in one lock), then wait_for_new_rollout (notify::recommended_watcher on today's dir, loops on Create/Modify events, also uses claim_unclaimed_rollout for race-safe attribution), then tail_rollout. Prevents multiple sessions from claiming the same rollout file. Attributed path written to codex-rollout-path.txt in session data dir.
  - notify-based watcher pattern in codex_rollout.rs: start_codex_rollout Tauri command creates a CodexRolloutHandle (oneshot stop channel), inserts it into CodexRolloutState registry before spawn (prevents start/stop race), then spawns run_watcher. run_watcher calls claim_unclaimed_rollout first (atomic: lock shared claimed_rollouts Arc<Mutex<HashSet>>, find fresh file, insert to claimed set in one lock), then wait_for_new_rollout (notify watcher on today's dir, loops on Create/Modify events, also uses claim_unclaimed_rollout for race-safe attribution), then tail_rollout. Both watchers built via build_watcher() (CR-04) which selects backend by OS — notify::PollWatcher on Windows / notify::RecommendedWatcher elsewhere — so OneDrive-redirected ~/.codex paths still surface events. Prevents multiple sessions from claiming the same rollout file. Attributed path written to codex-rollout-path.txt in session data dir.
- [CR-03 L451] start_codex_rollout / stop_codex_rollout Tauri commands manage CodexRolloutState (Mutex<HashMap<session_id, Arc<CodexRolloutHandle>>>). start_codex_rollout inserts the handle before tokio::spawn to guarantee stop_codex_rollout can always find and stop the watcher. stop_codex_rollout removes and drops the handle, sending stop signal. Called by TerminalPanel on session spawn/exit (stop_codex_rollout invoked in handlePtyExit).

## Codex Rollout Tap Bridge

- [CX-02 L701] Codex rollout watcher captures system + developer + user instructions for the model context viewer. session_meta.payload.base_instructions.text is captured into CodexPromptCaptureState.base_instructions (per-session, in tail_rollout's loop). turn_context payload feeds emit_codex_prompt_capture which extracts model + developer (collaboration_mode.settings.developer_instructions) + user (user_instructions); a stable capture key (model + base + developer + user joined with U+001F) dedupes repeated captures so identical turn_contexts don't re-emit. emit_tap_entry pushes a 'system-prompt' tap entry with text=base + messages=[developer?, user?] for the unified prompt capture pipeline. record_backend_event also logs 'codex.system_prompt' on initial capture and 'codex.prompt_capture' on each emit.
  - src-tauri/src/observability/codex_rollout.rs:L376 (CodexPromptCaptureState struct), src-tauri/src/observability/codex_rollout.rs:L454 (session_meta base_instructions capture), src-tauri/src/observability/codex_rollout.rs:L504 (turn_context calls emit_codex_prompt_capture), src-tauri/src/observability/codex_rollout.rs:L592 (emit_codex_prompt_capture body)
- [CX-01 L795] emit_tap_entry in codex_rollout.rs publishes per-session 'tap-entry-{sid}' Tauri events with cat fields so the unified tap pipeline drives Codex. Rollout line types map to cats: session_meta->codex-session (also fires SessionRegistration-like), session_configured(thread_name present)->codex-thread-name-updated (emits codexSessionId+threadName), turn_context->codex-turn-context, token_count event_msg->codex-token-count (with info and rateLimits), exec_command_end event_msg->codex-tool-call-complete, function_call/custom_tool_call response_item->codex-tool-call-start + codex-tool-input (dual emit), function_call_output/custom_tool_call_output->codex-tool-call-complete, message->codex-message, compacted->codex-compacted. function_call and custom_tool_call are handled identically; arguments falls back to input field.
- [CX-03 L1080] Codex task lifecycle events (task_started/turn_started, task_complete/turn_complete, turn_aborted) emit_event_msg branch produces dedicated tap entries (codex-task-started/codex-task-complete/codex-turn-aborted) and record_backend_event logs (codex.task_started/codex.task_complete/codex.turn_aborted). Frontend tapClassifier maps these to CodexTaskStarted, CodexTaskComplete, and UserInterruption events. Without these the Codex tab would never transition out of toolUse on natural turn end.
  - src-tauri/src/observability/codex_rollout.rs:L662 (task_started/turn_started arm), src-tauri/src/observability/codex_rollout.rs:L685 (task_complete/turn_complete arm), src-tauri/src/observability/codex_rollout.rs:L708 (turn_aborted arm), src/lib/tapClassifier.ts:L1100 (classifier)
