---
paths:
  - "src/components/Icons/Icons.tsx"
---

# src/components/Icons/Icons.tsx

Tag line: `L<n>`; code usually starts at `L<n+1>`.

## Data Flow

- [DF-08 L3] Icons module: src/components/Icons/Icons.tsx exports 37 inline SVG icon components (stroke-based, 16x16 viewBox, currentColor inheritance, pointerEvents none). Added in 8d454f3: IconResponse (speech bubble, tab icon for Response view), IconSession (three horizontal lines, tab icon for Session view). Includes scope icons: IconUser (person silhouette), IconBraces (curly brackets), IconTerminal (terminal prompt). No dependencies. All UI icons are monochrome SVGs.
