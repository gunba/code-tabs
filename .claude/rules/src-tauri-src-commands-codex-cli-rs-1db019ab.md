---
paths:
  - "src-tauri/src/commands/codex_cli.rs"
---

# src-tauri/src/commands/codex_cli.rs

Tag line: `L<n>`; code usually starts at `L<n+1>`.

## Codex CLI Introspection

- [CO-01 L192] discover_codex_models uses 'codex debug models' JSON output. Response envelope: {models: [{slug, display_name?, description?, default_reasoning_level?, supported_reasoning_levels: [{effort, description?}], visibility?, priority?, supported_in_api}]}. Filters out models where visibility != 'list' (hidden models excluded). Result drives the model picker in LaunchOptions.
- [CO-02 L284] discover_codex_cli_options uses 'codex --help' regex parsing (parse_help_options). Regex matches lines with 2+ leading spaces followed by optional short flag (-X), long flag (--word), and optional value placeholder (<VAL> or [VAL]). Indented continuation lines are appended to the preceding option's description (up to 200 chars). Non-indented non-empty lines (section headers) end continuation. Result: Vec<CodexCliOption {flag, short, description, takes_value}>.
- [CO-03 L536] CODEX_SLASH_COMMANDS is a vendored catalog of Codex TUI slash commands (35+ entries, expanded from 25 to include /fast, /experimental, /memories, /collab, /agent, /side, /ps, /stop, /personality, /realtime, /settings, /subagents, /feedback, /setup-default-sandbox, /sandbox-add-read-dir, /fork, /new, /rename). Vendored because Codex doesn't expose slash commands via CLI (binary is stripped Rust; no subcommand to list them). Exposed via discover_codex_slash_commands Tauri command. Last verified against codex-rs/tui/src/slash_command.rs.
- [CO-04 L731] discover_codex_settings_schema Tauri command returns CodexSchemaResult { schema, source: 'binary' | 'bundled' }. Best-effort mines the ConfigToml JSON Schema (Draft-07) from the resolved Codex native binary by scanning for the literal '"$schema": "http://json-schema.org/draft-07/schema#"' and validating each candidate object has all four signature keys (model_providers, mcp_servers, profiles, shell_environment_policy). Falls back to the vendored copy at src-tauri/src/discovery/codex_schema.json (refresh via npm run discover:fetch-codex-schema). Codex 0.125.0 doesn't ship the schema in the runtime binary so the fallback always fires today; future-proofed for when it does.
- [CO-05 L755] discover_codex_env_vars Tauri command mines CODEX_* identifiers as raw bytes from the resolved Codex native binary (no UTF-8 decode of the ~196 MiB file) using memchr::memmem on prefix b'CODEX_'; rejects mid-identifier hits (preceded by [A-Za-z0-9_]); walks forward up to 40 chars accepting [A-Z0-9_] and requires >=3 chars after PREFIX. Merges mined names with codex_env_var_catalog() (~45 curated entries spanning auth/runtime/sandbox/network/exec/cloud-tasks/debug categories with human descriptions). Curated entries set documented=true; mined-only entries default category='advanced' documented=false. is_noise_env_var() filters test-only Codex internals (e.g. CODEX_RS_SSE_FIXTURE, CODEX_*_OVERRIDE).
- [CO-06 L910] generate_codex_session_title Tauri command spawns 'codex exec --model X --ephemeral --output-last-message FILE --color never -c sandbox_mode=read-only -c approval_policy=never' as a one-shot to summarize the first user message into a 3-5 word tab title. Reuses Codex's own auth (file/keyring/env, both API key and ChatGPT OAuth) so the user never re-enters credentials — provider-symmetric with Claude Code's Haiku-driven CustomTitle. approval_policy=never (rather than deprecated on-failure) is chosen because codex exec is non-interactive (per AskForApproval enum doc in codex-rs/protocol/src/protocol.rs); combined with sandbox_mode=read-only any tool-write attempt fails inside the sandbox without surfacing prompts. 60s tokio::time::timeout, 4000-char prompt cap (TITLE_PROMPT_MAX_CHARS), 64-char output cap (TITLE_MAX_CHARS). Tempfile via uuid::Uuid::new_v4() under std::env::temp_dir() with RAII Drop guard removing it on every error path; current_dir set to system temp so any stray tool sees nothing interesting. Output cleanup via cleanup_title(): take first non-empty line, strip surrounding matching quotes (single, double, backtick), trim trailing .!?,;: punctuation, cap by char count (multibyte-safe). Returns Err on empty/timeout/non-zero exit so the frontend silently falls back to the heuristic name.

## Codex CLI Discovery

- [CL-01 L797] discover_codex_skills_sync scans skills in priority order: ~/.agents/skills (preferred global), <project>/.agents/skills (preferred per-project), then compat roots ~/.codex/skills + <project>/.codex/skills. dedup via seen-set keeps the first-occurring skill name. discover_codex_slash_commands reads the installed Codex CLI binary asynchronously (spawn_blocking the file read); searches with memchr::memmem::find for the embedded slash-command list. cmds.len() empty triggers fallback static catalog (no magic count threshold). save_codex_hooks merges config.toml + hooks.json hooks via merge_hook_values content-equal dedup; rewrites hooks.json to '{}' on present-but-empty save. /sandbox-add-read-dir is Windows-gated since Codex's TUI doesn't expose it on Windows builds.
- [CL-02 L834] /sandbox-add-read-dir is gated to Windows only via codex_slash_command_visible_on_platform(), mirroring codex-rs/tui/src/slash_command.rs:214. Both the binary-probe filter path and the fallback vendored-list path run through this gate. Codex has expanded the vendored catalog to 35+ commands including /fast, /experimental, /memories, /collab, /agent, /side, /new, /fork, /rename, /ps, /stop, /personality, /realtime, /settings, /subagents, /feedback, /setup-default-sandbox.
