---
paths:
  - "src/components/ActivityPanel/ActivityPanel.tsx"
---

# src/components/ActivityPanel/ActivityPanel.tsx

Tag line: `L<n>`; code usually starts at `L<n+1>`.

## Activity Panel

- [AP-04 L18] ActivityPanel shows a floating/sticky mascot that travels to the file currently being accessed by the main agent. The mascot is provider-aware (cli prop drives MASCOT_SRC: claude-mascot.png or codex-mascot.png — pre-rename agent-mascot.png is gone). Animations: reading (rocking), writing (rocking), moving (hop on path change), idle (subtle bob). Mascot animation is suppressed on tab switch: StickyMascot carries a tabId, and the floating mascot div is only rendered when mascot.tabId === activeTabId. On tab switch the stale mascot unmounts, preventing the CSS transition from animating the jump between tabs; the effect re-runs on activeTabId change and mounts a fresh mascot at the new tab's position. Mascot persists within a session across idle periods. Subagent files show inline indicators at their last-touched files while active, and completed subagents remain dimmed at their last-touched files. Subagent indicators render <AgentTypeIcon> (Compass / Clipboard / Sparkles / BookOpen / Terminal / ShieldCheck / Bot fallback) inside the same .agent-mascot-img wrapper so the rock/hop/bob animations and overlay emoji apply identically; only main-agent indicators use the provider mascot artwork. .agent-mascot-icon tints the SVG with var(--cli-claude). Tree indentation uses INDENT_STEP=16px.
- [AP-05 L19] ActivityPanel has two view modes toggled by a button group: 'Response' (files touched since lastUserMessageAt, filtered per-file by timestamp) and 'Session' (all visited paths via allFiles/visitedPaths). File state communicated through filename color only: created=green (--success), modified=yellow (--warning), deleted=red+strikethrough (--error), read=muted (--text-secondary) via file-tree-kind-* CSS classes. No letter badges.
- [AP-06 L313] Searched-color agent precedence: (1) Write-side in addFileActivity() — turn-dedup branch (src/store/activity.ts:L183) and allFiles branch (activity.ts:L213) both skip a subagent searched entry when a main-agent (agentId=null) entry already exists for the same path. (2) Render-side in ActivityPanel.tsx — file-tree-searched CSS applied to a folder only when node.activity.kind==='searched' AND (agentId is null/undefined — main agent always wins — OR agentId is still in the active subagent set), AND !node.isWorkspaceRoot (workspace root accent must win over search overlay). When a subagent is removed via removeSubagent(), clearAgentSearchActivity() drops all {kind:'searched',isFolder:true,agentId:X} entries from turns and allFiles. Both layers ensure subagent searches never override or outlive main-agent search coloring.
- [AP-07 L379] ActivityPanel renders a header band (.activity-panel-header) above the scrollable tree: a 32px AgentMascot with hideOverlay=true sitting on a pulsing radial-gradient clay glow (panel-header-glow-pulse keyframes, 4s ease-in-out), followed by a two-line narration. Line 1 is verb + kind-tinted target chip (.panel-header-target with .file-tree-kind-{created|modified|deleted|read|searched} background tints) + optional 'Ns ago' detail in idle. Line 2 is the workspace-relative path in mono (.file-tree-path-mono → var(--font-mono)). Verbs derive from currentToolName via toolToVerb(): Read→Reading, Edit/NotebookEdit→Editing, Write→Writing, Grep/Glob/LSP→Searching, Bash→Running. Idle fallback uses lastMainAgentFile.kind for the chip color and relativeTime() for the detail. Bash never reaches the chip path because Bash is excluded from FILE_TOOLS in primaryActive resolution; falls through to idle narration. workspaceRelative() strips the canonicalized workspace prefix when present.
- [AP-08 L476] ActivityPanel renders a translucent breadcrumb of recent main-agent file positions inside .activity-tree-container. recentMainAgentPaths walks activity.turns newest-first to collect the last FOOTPRINT_LIMIT+1 (=6) distinct main-agent (agentId=null) paths. The footprint-positioning effect filters out the current floating-mascot target, then renders up to 5 .activity-footprint dots positioned absolutely at the matching row's left margin (data-path query, depth-driven left). Opacity steps 0.42 → 0.08 by age via Math.max(0.08, 0.42 - age*0.08); the age counter increments on collapsed/missing rows so visible footprints retain temporal ordering. Cleared on tab switch alongside expandedFiles via the activeTabId useEffect, and re-derived in the same effect pattern as the floating mascot when rows/depthByPath change.
- [AP-09 L763] ActivityPanel changes the file-row click handler from invoke('shell_open') to toggling local expandedFiles Set state; shell_open moves to a dedicated .file-tree-open-btn (IconExternalLink) on the right edge with stopPropagation. Expanded rows render <DiffCard> directly underneath the row, indented at depth*INDENT_STEP+24px. buildUnifiedDiff splits oldString/newString on newline, walks longest common prefix and suffix, emits up to 2 lines of context before/after the diff range, marks removed lines red and added lines green via .file-tree-diff-{context|added|removed} with -/+ gutter glyphs, and caps total at 24 lines. Write activities render the first 24 lines of toolInputData.content as additions. Read activities show 'No diff captured for reads.' calculateDelta runs the same prefix/suffix walk to produce '+N −M' summaries in the per-row metadata cluster (.file-tree-meta) alongside a 1Hz-ticked relativeTime label and a hover-revealed external-link button. Expanded files are local React state and reset on tab switch.
